No generative AI was used to make this assignment.


For the criterion:

Number of pages - 
For the purposes of this assignment, I assert that this website is worth 8+ 'pages' worth of matierial.
For the breakdown of how I came to this conclusion, consider:
This website is made of only four individual web pages, however, two contain plenty of important content stored within collapsible tabs. This website was designed this way so a user would not have to switch pages and have to reload them again and again, and as such should be included in the overall page count to provide a lower bound on the page count.
Thus, I conclude that this website would fit within the expected bounds of matierial that the assignment has decided.

Properly define CSS classes and headers (at least one external) - I defined many classes in order to make this site with the css file and have used it across multiple 

Layout - I have used Javascript to ensure that my headers and footers maintain a conistent design across all webpages.

W3C validation - 

Good responsive web design - 

Use of meaningful content(text) - Although not all of the content is still here, the overall presentation and layout of information has been designed to allow for further expansion of its core concepts.

Image/s - The five images included are as follows: the favicon (world logo,) the background, and the three biome buttons.


Outside of this, to consider for all extra components, I have added the following:
+ All headers and footers are generated by Javascript to provide easy changing to their structure across all webpages (Javascript)
+ Collapsible tab class (Javascript)
    - For loops across all class items
    - Event Listener
    - Transition CSS property
+ Tables that the user is able to roll on to produce a random result (Javascript)
    - For loops
    - Arrays
    - Random
    - Event Listener
+ Favicon (CSS)
+ Custom fonts (CSS)